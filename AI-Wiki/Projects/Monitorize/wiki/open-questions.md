# Monitorize Open Questions

Track project questions that future Codex sessions should resolve through repo inspection, tests, or user input.

## Product and Support

- What exact behavior is expected for GNOME and Sway before they stop being experimental?
- Which Linux distributions are official support targets beyond Fedora and Arch?
- What release criteria define "out of beta"?

## Architecture and Code

- Where are Android discovery, streaming, input, and security packages currently implemented in the source tree?
- Which Linux tests cover streaming lifecycle, virtual display safety, and input mapping?
- What packaging path is intended first: Flatpak, distro package, AppImage, or another format?
- Does `linux/monitorize/desktop/tray_agent.py` need an `import os` fix before the lightweight tray agent can run?

## Documentation

- Which setup steps should be duplicated in app UI versus kept only in docs?
- Should the wiki track user-facing release notes separately from implementation notes?
